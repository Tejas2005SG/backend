{
  "name": "backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "generate": "prisma generate",
    "migrate": "prisma migrate deploy"
  },
  "devDependencies": {
    "prisma": "^5.0.0"
  }
}
